"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import AppShell from "@/components/period/AppShell"; import StatusBar from "@/components/period/StatusBar"; import SetupHeader from "@/components/period/SetupHeader";
export default function SetupHeightPage(){ const router = useRouter(); const [height,setHeight]=useState("165"); const [unit,setUnit]=useState("cm"); return <AppShell><StatusBar /><section className="card stack"><SetupHeader step={4} total={7} backHref="/setup/weight" title="How tall are you?" description="Your height helps us complete your setup and prepare a better personalized health profile." /><div className="split-2"><div className="input-wrap"><label className="label">Height</label><input className="input" type="number" min="1" value={height} onChange={(e)=>setHeight(e.target.value)} /></div><div className="input-wrap"><label className="label">Unit</label><select className="select" value={unit} onChange={(e)=>setUnit(e.target.value)}><option value="cm">cm</option><option value="ft">ft</option></select></div></div><button className="primary-button" onClick={()=>router.push('/setup/period-length')} disabled={!height}>Continue</button></section></AppShell>; }
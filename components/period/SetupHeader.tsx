import Link from "next/link"; export default function SetupHeader({ step,total,backHref,title,description }:{ step:number; total:number; backHref:string; title:string; description:string; }){ const width = `${(step/total)*100}%`; return <div className="stack" style={{ gap: 14 }}><div style={{ display:"flex", alignItems:"center", justifyContent:"space-between" }}><Link href={backHref} className="ghost-button" style={{ width:56 }}>←</Link><span className="label">Step {step} of {total}</span></div><div className="progress"><span style={{ width }} /></div><div><h1 className="page-title">{title}</h1><p className="page-subtitle" style={{ marginTop:10 }}>{description}</p></div></div>; }
